import { createSlice } from '@reduxjs/toolkit';

const blockchain-explorerSlice = createSlice({
  name: 'blockchain-explorer',
  initialState: {},
  reducers: {},
});

export default blockchain-explorerSlice.reducer;
